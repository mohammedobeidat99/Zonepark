<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/zonepark_asset"
        android:label="ZonePark"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".Admin.Delete"
            android:exported="false" />
        <activity
            android:name=".User.userMain"
            android:exported="false" />
        <activity
            android:name=".User.startUser"
            android:exported="false" />
        <activity
            android:name=".Admin.cities"
            android:exported="false" />
        <activity
            android:name=".Admin.register"
            android:exported="false" />
        <activity
            android:name=".Admin.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>